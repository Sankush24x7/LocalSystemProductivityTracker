<Window x:Class="ProductivityTracker.App.PinPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Unlock"
        Height="230"
        Width="420"
        Background="#101A2A"
        Foreground="#EAF0FF"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="PIN Required" FontSize="24" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" x:Name="HintText" Margin="0,8,0,8" Foreground="#AFC5E6" TextWrapping="Wrap" />

        <PasswordBox Grid.Row="2" x:Name="PinBox" Height="34" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Width="100" Height="32" Margin="0,0,8,0" Content="OK" Click="Ok_Click" />
            <Button Width="100" Height="32" Content="Cancel" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
