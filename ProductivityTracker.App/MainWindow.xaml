<Window x:Class="ProductivityTracker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AI Productivity Tracker"
        Icon="pack://application:,,,/Assets/AppIcon.png"
        Height="780"
        Width="1240"
        WindowStartupLocation="CenterScreen"
        Background="#0D1119"
        Foreground="#EAF0FF">
    <Window.Resources>
        <SolidColorBrush x:Key="CardBrush" Color="#141B28" />
        <Style TargetType="Border">
            <Setter Property="CornerRadius" Value="12" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#253042" />
            <Setter Property="Foreground" Value="#EAF0FF" />
            <Setter Property="BorderBrush" Value="#324156" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#101723" />
            <Setter Property="Foreground" Value="#EAF0FF" />
            <Setter Property="BorderBrush" Value="#2B3A4E" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Margin" Value="4" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#EAF0FF" />
            <Setter Property="Foreground" Value="#0B1220" />
            <Setter Property="BorderBrush" Value="#2B3A4E" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="IsEditable" Value="True" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#EAF0FF" />
            <Setter Property="Foreground" Value="#0B1220" />
            <Setter Property="Padding" Value="6,4" />
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#2E6695" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#45C4B0" />
                    <Setter Property="Foreground" Value="#0B1220" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#0F1826" />
            <Setter Property="Foreground" Value="#EAF0FF" />
            <Setter Property="BorderBrush" Value="#2B3A4E" />
            <Setter Property="RowBackground" Value="#0F1826" />
            <Setter Property="AlternatingRowBackground" Value="#122036" />
            <Setter Property="HorizontalGridLinesBrush" Value="#25364F" />
            <Setter Property="VerticalGridLinesBrush" Value="#25364F" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1A2940" />
            <Setter Property="Foreground" Value="#EAF0FF" />
            <Setter Property="BorderBrush" Value="#2B3A4E" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8,6" />
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="#EAF0FF" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2E6695" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                    <Setter Property="Foreground" Value="#68E08E" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                    <Setter Property="Foreground" Value="#FFD166" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                    <Setter Property="Foreground" Value="#FF6C79" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="#EAF0FF" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#25364F" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="6,4" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2E6695" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Background="{StaticResource CardBrush}" Padding="16" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock FontSize="24" FontWeight="SemiBold" Text="Today's Productivity Dashboard" />
                    <TextBlock Margin="0,4,0,0" Foreground="#9FB3CF" Text="Runs silently in tray. Press Ctrl+Shift+T anytime to open this dashboard." />
                    <TextBlock Margin="0,4,0,0" Foreground="#78D7C7" FontWeight="SemiBold" Text="Created by Sandeep Kushwaha (SanKush)" />
                </StackPanel>
                <WrapPanel Grid.Column="1" VerticalAlignment="Center">
                    <Button Content="New Task" Click="NewTaskButton_Click" />
                    <Button Content="Refresh" Click="RefreshButton_Click" />
                    <Button Content="Generate Report" Click="GenerateReportButton_Click" />
                    <Button Content="Send Report" Click="SendReportButton_Click" />
                    <Button Content="Manage Reports" Click="ManageReportsButton_Click" />
                    <Button Content="Trends" Click="TrendsButton_Click" />
                    <Button x:Name="PauseResumeButton" Content="Pause Tracking" Click="PauseResumeButton_Click" />
                    <Button Content="Backup" Click="BackupButton_Click" />
                    <Button Content="Restore" Click="RestoreButton_Click" />
                </WrapPanel>
            </Grid>
        </Border>

        <UniformGrid Grid.Row="1" Columns="7" Margin="0,12,0,12">
            <Border Background="{StaticResource CardBrush}" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Foreground="#97AAC5" Text="Total Tasks" />
                    <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding TotalTasks}" />
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBrush}" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Foreground="#97AAC5" Text="Completed" />
                    <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding CompletedTasks}" />
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBrush}" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Foreground="#97AAC5" Text="Pending" />
                    <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding PendingTasks}" />
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBrush}" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Foreground="#97AAC5" Text="Productivity Score" />
                    <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding ProductivityScore, StringFormat={}{0}%}" />
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBrush}" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Foreground="#97AAC5" Text="Task Points" />
                    <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding TaskPoints}" />
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBrush}" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Foreground="#97AAC5" Text="Active Time" />
                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding ActiveTime}" />
                </StackPanel>
            </Border>
            <Border Background="{StaticResource CardBrush}" Margin="4" Padding="12">
                <StackPanel>
                    <TextBlock Foreground="#97AAC5" Text="Idle Time" />
                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding IdleTime}" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*" />
                <ColumnDefinition Width="1.1*" />
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource CardBrush}" Margin="0,0,8,0" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <WrapPanel Grid.Row="0" Margin="0,0,0,8" VerticalAlignment="Center">
                        <TextBox x:Name="TaskSearchTextBox" Width="220" ToolTip="Search title/description" TextChanged="TaskSearchTextBox_TextChanged" />

                        <ComboBox x:Name="FilterStatusComboBox" Width="130" Margin="4" SelectionChanged="FilterControl_Changed" IsEditable="True" IsReadOnly="True">
                            <ComboBoxItem Content="All Status" />
                            <ComboBoxItem Content="Pending" />
                            <ComboBoxItem Content="Running" />
                            <ComboBoxItem Content="Completed" />
                        </ComboBox>

                        <ComboBox x:Name="FilterPriorityComboBox" Width="130" Margin="4" SelectionChanged="FilterControl_Changed" IsEditable="True" IsReadOnly="True">
                            <ComboBoxItem Content="All Priority" />
                            <ComboBoxItem Content="Low" />
                            <ComboBoxItem Content="Medium" />
                            <ComboBoxItem Content="High" />
                        </ComboBox>

                        <CheckBox x:Name="FilterDueTodayCheckBox" Content="Due Today" Margin="8,6,0,0" Checked="FilterControl_Changed" Unchecked="FilterControl_Changed" />
                        <CheckBox x:Name="FilterOverdueCheckBox" Content="Overdue" Margin="8,6,0,0" Checked="FilterControl_Changed" Unchecked="FilterControl_Changed" />

                        <Button Content="Clear Filters" Margin="8,2,0,0" Click="ClearFiltersButton_Click" />
                    </WrapPanel>

                    <DataGrid Grid.Row="1"
                              x:Name="TasksDataGrid"
                              ItemsSource="{Binding Tasks}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              SelectionMode="Single"
                              SelectionChanged="TasksDataGrid_SelectionChanged"
                              CanUserAddRows="False"
                              Background="#111824"
                              BorderBrush="#2B3A4E"
                              Foreground="#ECF3FF">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="1.3*" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="1.8*" />
                            <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="*" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                            <DataGridTextColumn Header="Created" Binding="{Binding CreatedTime, StringFormat={}{0:g}}" Width="*" />
                            <DataGridTextColumn Header="Due" Binding="{Binding DueTime, StringFormat={}{0:g}}" Width="*" />
                            <DataGridTextColumn Header="TAT" Binding="{Binding TatDisplay}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <StackPanel Grid.Column="1">
                <Border Background="{StaticResource CardBrush}" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" FontSize="16" Text="Task Editor" Margin="0,0,0,10" />
                        <TextBox x:Name="TitleTextBox" ToolTip="Task title" />
                        <TextBox x:Name="DescriptionTextBox" ToolTip="Description" Height="70" TextWrapping="Wrap" AcceptsReturn="True" />
                        <ComboBox x:Name="PriorityComboBox" IsEditable="True" IsReadOnly="True" />
                        <ComboBox x:Name="StatusComboBox" IsEditable="True" IsReadOnly="True" />
                        <DatePicker x:Name="DueDatePicker" Margin="4" />
                        <TextBox x:Name="DueTimeTextBox" Text="18:00" ToolTip="Due time (HH:mm)" />
                        <WrapPanel Margin="0,6,0,0">
                            <Button Content="Save" Click="SaveTaskButton_Click" />
                            <Button Content="Delete" Click="DeleteTaskButton_Click" />
                            <Button Content="Mark Completed" Click="MarkCompletedButton_Click" />
                            <Button Content="Clear" Click="ClearFormButton_Click" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource CardBrush}" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" FontSize="16" Text="Smart Suggestion" />
                        <TextBlock TextWrapping="Wrap" Margin="0,8,0,0" Text="{Binding AiSuggestion}" Foreground="#B9D3FF" />
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource CardBrush}" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" FontSize="16" Text="Top Applications" />
                        <ItemsControl ItemsSource="{Binding TopApps}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="0,6,0,0">
                                        <TextBlock DockPanel.Dock="Right" Foreground="#A9BEE1" Text="{Binding DurationLabel}" />
                                        <TextBlock Text="{Binding ApplicationName}" TextTrimming="CharacterEllipsis" />
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource CardBrush}" Padding="12">
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" FontSize="16" Text="Health Check" />
                        <TextBlock Margin="0,8,0,0" Text="{Binding HealthTracking}" Foreground="#B9D3FF" />
                        <TextBlock Margin="0,4,0,0" Text="{Binding HealthHotkeys}" Foreground="#B9D3FF" />
                        <TextBlock Margin="0,4,0,0" Text="{Binding HealthStartup}" Foreground="#B9D3FF" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <Border Grid.Row="3" Background="#121A28" Padding="8" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusText}" Foreground="#93A8C4" />
        </Border>
    </Grid>
</Window>






